normal

```vcl
func("a")
```

with whitespace

```vcl
func  ("a")
```

with multiple args

```vcl
func("a", "b")
```

`if` func

```vcl
if("a", "b", "c")
```

## abnormal

trailing comma in args

```vcl
func("a",)
```

## lint

## format
