{
  "name": "web",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "private": true,
  "author": "Sho Miyamoto <me@shomiyamoto.dev>",
  "main": "dist/index.js",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "rimraf dist && cp -r src/public dist && yarn webpack --watch --dev --config webpack.config.ts",
    "build": "rimraf dist && cp -r src/public dist && yarn webpack --config webpack.config.ts",
    "prepublishOnly": "yarn build && yarn gh-pages -d dist",
    "lint": "eslint src --ext .ts",
    "type": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "yargs": "^13.2.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/shqld/vaceline.git",
    "directory": "web"
  }
}
